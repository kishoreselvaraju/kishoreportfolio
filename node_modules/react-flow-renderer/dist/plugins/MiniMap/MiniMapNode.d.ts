/// <reference types="react" />
import { Node } from '../../types';
interface MiniMapNodeProps {
    node: Node;
    color: string;
    borderRadius: number;
}
declare const MiniMapNode: {
    ({ node, color, borderRadius }: MiniMapNodeProps): JSX.Element;
    displayName: string;
};
export default MiniMapNode;
